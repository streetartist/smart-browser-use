fn main() {
    eprintln!(
        "smart-browser-use interactive control is provided by the MCP server.\n\
Run: mcp-server --headed\n\
or:  mcp-server --headed --transport http --port 3000"
    );
}
